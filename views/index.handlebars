<div class = "container">
    <div class = "row">
        <div class = "col-12">
            <h1>Welcome to Burgerville!</h1>
        </div>
    </div>
    <div class = "row menu">
        <div class = "col-6">
            <h2>Available Burgers: </h2>
            <ul class = "list-group">
                {{#each burger}}
                    {{#unless devoured}}
                    <li class = "list-group-item" id = "{{id}}">{{burger_name}} <button data-value ="{{id}}" class = "devour btn btn-primary">Eat Me!</button></li>
                    {{/unless}}
                {{/each}}
            </ul>
        </div>
        <div class = "col-6">
            <h2>Burger History: </h2>
            <ul class = "list-group">
                {{#each burger}}
                    {{#if beenEaten}}
                    <li class = "list-group-item" id = "{{id}}">{{burger_name}} 
                        {{#if devoured}}
                        <button data-value ="{{id}}" class = "revive btn btn-primary">Make Me Again!</button>
                        {{/if}}
                        <div class = "row">
                            <small>Burgers Eaten: {{eaten}}</small>
                        </div>
                    </li>
                    {{/if}}
                {{/each}}
            </ul>
        </div>
    </div>
    <div class = "row add">
        <div class = "col-12 add2">
            <h5>Make a New Burger</h5>
            <form>
                <div class="form-group">
                    <input type="text" class="form-control" id="new-burger" placeholder="Name Your Burger" >
                </div>
                <button class="btn btn-primary" id = "add-burger">Add Burger</button>
            </form>
        </div>
        
    </div>
</div>
